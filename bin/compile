#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

echo "-----> Installing dbus 1.8.0"
cd $1
curl http://s3-us-west-2.amazonaws.com/tmpxkm/dbus.tar.gz -s -O
mkdir -p vendor/dbus
tar -C vendor/dbus -xf dbus.tar.gz
